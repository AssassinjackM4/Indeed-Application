# MongoDB Atlas Connection
# Use environment variable SPRING_DATA_MONGODB_URI for deployment
# For local dev, set in system env or use default MongoDB connection
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb+srv://user:mongoindeed@indeed-application.2cnjzm4.mongodb.net/?retryWrites=true&w=majority&appName=Indeed-Application}
spring.data.mongodb.database=${SPRING_DATA_MONGODB_DATABASE:indeeddb}
spring.data.mongodb.auto-index-creation=true

# Server Configuration
server.port=${PORT:8080}
server.error.whitelabel.enabled=false

# Spring configuration
spring.config.additional-location=classpath:/application.properties

