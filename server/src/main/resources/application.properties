# MongoDB Atlas Connection
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb+srv://user:mongoindeed@indeed-application.2cnjzm4.mongodb.net/Indeed-Application?retryWrites=true&w=majority&appName=Indeed-Application}
spring.data.mongodb.database=${SPRING_DATA_MONGODB_DATABASE:Indeed-Application}
spring.data.mongodb.auto-index-creation=true

# Server Configuration
server.port=${PORT:10000}
server.servlet.context-path=/
server.error.whitelabel.enabled=false

# Logging
logging.level.root=INFO
logging.level.com.indeed.server=DEBUG

# Application Name
spring.application.name=indeed-server

